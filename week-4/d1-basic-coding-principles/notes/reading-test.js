/*
//contains a string with the function we will be testing for
describe("avgValue()", function () {

    //has a descriptive string of what the function should be doing
    it("should return the average of an array of numbers", function () {

        //should return the average of an array
        assert.equal(avgValue([10, 20]), 15);
    });
});
*/